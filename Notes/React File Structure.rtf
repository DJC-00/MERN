use npx create-react-app <APP_NAME> to create a new react project

in src/App.js:
    - add import React from 'react';
    - everything inside the <div className="App"> can be deleted

inside the src folder, create a components folder
    - Components can be though of as self contained objects that have all their own styling and JS Code